import { assert, expect, test } from 'vitest'
import RegexBuilder from './RegexBuilder.ts'
import RegexSwitchGroupNameBuilder from './RegexSwitchGroupNameBuilder.ts'
import { IEachBuilderRes } from './IEachBuilder.ts'

test('elseThrow', () => {
    // const kv = new Map<string, string>()
    // kv.set('groupName', 'groupNameValue')

    // const c = new RegexSwitchGroupNameBuilder({} as RegexBuilder, "groupName")
    // c.elseThrow()
    // assert.throws(() => c.run(kv))
})

test('No error', () => {
//     const kv = new Map<string, string>()
//     kv.set('groupName', 'groupNameValue')

//     const c = new RegexSwitchGroupNameBuilder({} as RegexBuilder, "groupName")
//     assert.doesNotThrow(() => c.run(kv))
})

